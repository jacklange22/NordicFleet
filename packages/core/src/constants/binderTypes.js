// Binder products used as the first layer of klister-base waxing for
// classic skis. "None" is the implicit default and is normalized to null
// by the validator when stored.

const BINDER_TYPES = Object.freeze([
  'None',
  'VG Swix',
  'Toko Base',
  'GS Base AT',
  'GS Base Super',
  'Chola',
  'K-base',
  'Rode Blackbase',
  'KS base',
]);

module.exports = {BINDER_TYPES};
