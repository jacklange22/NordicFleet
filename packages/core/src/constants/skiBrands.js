// Curated brand list used by the AddSki pill selector. "Other" reveals a
// free-text input. Order is intentional — most common nordic brands first.

const SKI_BRANDS = Object.freeze([
  'Fischer',
  'Salomon',
  'Atomic',
  'Madshus',
  'Rossignol',
]);

module.exports = {SKI_BRANDS};
