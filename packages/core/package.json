{
  "name": "@nordicfleet/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared business logic for the NordicFleet platform (types, validation, constants, payload builders).",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/ --no-eslintrc -c .eslintrc.js"
  },
  "devDependencies": {
    "eslint": "^8.19.0",
    "jest": "^29.6.3"
  }
}
